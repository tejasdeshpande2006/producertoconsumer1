rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: open rules for debugging checkout/place-order flow.
    // Tighten these rules again before production release.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
